package com.redleefstudios.sushidictionary;

/**
 * Created by Fred on 4/14/2016.
 */
public enum Ingredient {

    SALMON, ALBACORE_TUNA, TUNA, YELLOWTAIL, EEL, MACKERAL, SEA_URCHIN, ALASKAN_SALMON, SALMON_SKIN, CRAB_MEAT, FISH_ROE, RAZOR_CLAM, SQUID, OCTOPUS;


    /*
    public static String[] names()
    {
        Category[] categories = values();
        String[] names = new String[categories.length];

        for(int i = 0; i < categories.length; i++)
        {
            names[i] = categories[i].name();
        }

        return names;
    }
    */
}
